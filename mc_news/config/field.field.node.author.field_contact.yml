langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_contact
    - node.type.author
    - node.type.organization
    - node.type.person
id: node.author.field_contact
field_name: field_contact
entity_type: node
bundle: author
label: Contact
description: 'Select a Person or Organization. Contact information associated with that content will pull into the article automatically.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:node'
  handler_settings:
    target_bundles:
      organization: organization
      person: person
    sort:
      field: _none
      direction: ASC
    auto_create: false
    auto_create_bundle: organization
field_type: entity_reference
